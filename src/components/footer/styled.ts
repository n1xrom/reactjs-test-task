import styled from "styled-components";

const Wrapper = styled.div`
  box-shadow: 0px -5px 5px -5px #1976d245;
  padding: 0 20px;
  display: grid;
  align-items: center;
  justify-content: space-around;
  grid-template-columns: repeat(2, 100px);
`;

export { Wrapper };
